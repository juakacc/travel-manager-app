import { StyleSheet } from 'react-native';

const comumStyles = StyleSheet.create({
  btn: {
    backgroundColor: '#ff0',
    padding: 10,
    marginTop: 10,
    borderRadius: 10,
  },
  btnText: {
    fontSize: 20,
  },
});

export default comumStyles;
